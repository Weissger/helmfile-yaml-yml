repositories:
  - name: jetstack
    url: https://charts.jetstack.io

releases:
  - name: cert-manager
    namespace: cert-manager
    chart: jetstack/cert-manager
    version: 1.8.1
  - name: cluster-issuer
    namespace: cert-manager
    chart: ./manifests